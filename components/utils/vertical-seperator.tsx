import styles from "@styles/components/utils/vertical-seperator.module.scss"


// This is a dumb component, only used to create a vertical line
// to visually seperate elements

const VerticalSeperator = () => {

    return <div className={styles.seperator}>p</div>
}

export default VerticalSeperator